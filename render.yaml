# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: laundry-oms
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /health
